# TypeScript Node.js Boilerplate Code

## RUN INSTRUCTIONS,
- Clone the directory
- Run `npm install` in project directory
- After, Run  `start:dev` in the project directory, 
- Then your project should start running on `http://localhost:3000`
